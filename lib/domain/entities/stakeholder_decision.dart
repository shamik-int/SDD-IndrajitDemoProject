/// Input to `OP04` (`recordStakeholderDecision`) — see the spec's Local Data
/// Contract. Backs the Simulate Decision control (AC14) only.
enum StakeholderDecision { approved, rejected, completed }
