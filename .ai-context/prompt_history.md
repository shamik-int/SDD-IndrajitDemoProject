# Prompt History — Session-Level Agent Audit Trail

_Distinct from `status.md` (human-curated daily summary). Appended after every
completed task per `.agent/rules/auto-log.md`._

### 2026-09-15 — Project scaffold
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Initial SDD workspace scaffold for the Employee Internal Transfer Digital Journey
- **Prompt summary:** Create `.agent/` and `.ai-context/` structure per INT SDD Blueprint §15; draft `constitution.md` and `BRD-001` from the assessment scope document (`Requirement for SDD (2).pdf`).
- **Files touched:** `.agent/rules/*`, `.agent/workflows/*`, `.ai-context/constitution.md`, `.ai-context/project_context.md`, `.ai-context/architecture.md`, `.ai-context/BRD.md`, `.ai-context/status.md`
- **Outcome:** Scaffold created. No spec/plan/tasks yet — next step is authoring `specs/employee-internal-transfer.spec.md`.

### 2026-09-15 — Client architecture decision
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Record Flutter client architecture (Clean Architecture + GetX, local-first persistence, theming, responsiveness, envs, mobile security hardening) as formal ADRs.
- **Prompt summary:** Author `ADR-0001` (client architecture/state management/persistence/theming/responsiveness/env) and `ADR-0002` (root/jailbreak/Frida detection + memory hygiene); update `architecture.md` and `constitution.md` to reflect them; scaffold `assets/env|images|fonts|icons/`.
- **Files touched:** `.ai-context/decisions/ADR-0001-*.md`, `.ai-context/decisions/ADR-0002-*.md`, `.ai-context/architecture.md`, `.ai-context/constitution.md`, `.ai-context/decisions/README.md`, `assets/env/.env.uat`, `assets/env/.env.prod`, `assets/images/README.md`, `assets/fonts/README.md`, `assets/icons/README.md`
- **Outcome:** Two ADRs accepted. Flagged for Gate 1: (1) env files must stay secret-free — clarified in constitution.md rather than silently assumed; (2) `utils`/`constants` must physically live under `lib/core/` (Dart only compiles from `lib/`) — documented in `architecture.md`'s folder tree as the resolution of the "utils/constants in root" request.

### 2026-09-15 — AGENT.md gateway + project checklist
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Author `AGENT.md` (prompt-routing gateway) and `PROJECT_CHECKLIST.md` (working completion checklist spanning Flutter scaffolding + SDD gates + assessment deliverables).
- **Files touched:** `AGENT.md`, `README.md` (created — was missing from the earlier scaffold pass), `PROJECT_CHECKLIST.md`
- **Outcome:** Both created. Noted `README.md` had been planned but never actually written in the earlier scaffold — created it now.

### 2026-09-15 — Flutter project scaffolded (ADR-0001 / ADR-0002 build-out)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Scaffold the actual Flutter project and `lib/` Clean Architecture tree per ADR-0001/ADR-0002.
- **Prompt summary:** `flutter create` (android/ios/web); add `get`, `flutter_screenutil`, `hive`/`hive_flutter`, `flutter_dotenv`, `dio`, `equatable`, `path_provider`, `intl`, `freerasp`, `mocktail`; verified `freerasp`'s actual API (`ThreatCallback`, `TalsecConfig`) against the installed package source rather than guessing; built `core/{result,network,local_db,theme,responsive,utils,constants,security}`, `app/{app.dart,routes,bindings,shell}`, `main.dart`; left `data/`, `domain/`, `presentation/` as README placeholders (feature code waits for Tasks stage); added smoke test + unit tests for `Result` and `Validators`.
- **Files touched:** `pubspec.yaml`, `lib/**` (new), `test/**` (replaced default counter test, added `core/utils/validators_test.dart`, `core/result/result_test.dart`), `assets/fonts/*.ttf` (download attempted)
- **Outcome:** `flutter analyze` — no issues. `flutter test` — 12/12 passing. Flagged, not silently shipped: `SecurityService` uses placeholder signing-cert hash / iOS Team ID / watcher email and `isProd: false` — must be replaced with real values before any release build. No Android/iOS native build-flavor split yet (env selection is `--dart-define` only) — left open pending a Plan-stage decision on whether side-by-side UAT/PROD installs are needed.

### 2026-09-15 — Real Lato fonts wired in
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Replace the font placeholder with real Lato `.ttf` weights.
- **Prompt summary:** Downloaded Lato-Regular/Light/Bold/Italic from the Google Fonts GitHub repo into `assets/fonts/`, verified as valid TrueType data, uncommented the `fonts:` block in `pubspec.yaml`.
- **Files touched:** `assets/fonts/Lato-*.ttf` (new), `pubspec.yaml`
- **Outcome:** `flutter pub get` / `flutter analyze` / `flutter test` re-run clean (no issues, 12/12 passing). Flutter Project Scaffolding checklist section now fully complete except the intentionally-deferred items (build flavors, real security config).

### 2026-09-15 — employee-internal-transfer spec authored (Deliverable 2 & 3)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Author the feature spec and its spec-derived test cases.
- **Prompt summary:** Drafted `specs/employee-internal-transfer.spec.md` (Intent, Context, API Contract with 3 endpoints, 13 individually-IDed ACs, 13 spec-derived Unit Test Cases, Explicitly Out of Scope, Non-Functional Constraints) and `test_cases/employee-internal-transfer.test_cases.md` (mirrors the spec's UTs + 10 QA-added edge/negative cases). Resolved 7 of BRD-001's open questions with explicit, testable v1 defaults rather than leaving the spec ambiguous, and surfaced them in a dedicated "Assumptions Requiring Gate 1 Confirmation" section.
- **Files touched:** `.ai-context/specs/employee-internal-transfer.spec.md`, `.ai-context/test_cases/employee-internal-transfer.test_cases.md`, `.ai-context/test_cases/_integration.md`, `.ai-context/specs/README.md`, `.ai-context/status.md`, `AGENT.md`
- **Outcome:** Spec status set to **In Peer Review (v1.0)** — not Approved. Explicitly did not proceed to Plan authoring in the same turn: constitution.md Principle #1 and `AGENT.md`'s own routing rule gate Plan drafting on Gate 1 approval by Subhajit Mukherjee, which hasn't happened yet. Flagged to the user rather than silently skipped or silently proceeded.
- Also authored `.ai-context/discovery/employee-internal-transfer.discovery.md` (Deliverable 1), closing out Milestone 1 in full: business objective, primary users, journey stages, business rules, known decisions, open questions, assumptions, dependencies, out-of-scope, and an explicit business-vs-technical decision split.

### 2026-09-15 — Gate 1 self-review pass (per user's chosen option)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Perform a genuine self-review of employee-internal-transfer.spec.md v1.0 against the Blueprint §30 Gate 1 checklist, standing in for Subhajit Mukherjee's actual review (user-selected option: "Self-review pass first").
- **Prompt summary:** Read the spec critically rather than rubber-stamping it. Found 5 real issues: (1, blocking) no defined mechanism for how Manager/HR/Payroll/IT/Facilities actions get recorded — the spec's API contract is employee-facing only and never says so explicitly; (2, blocking-as-worded) the "HR eligibility placeholder rule" assumption doesn't gate any AC and would have blocked Approval on something irrelevant; (3, non-blocking) "non-terminal" used but never defined; (4, QA gap) AC4's four possible missing fields only had one test case; (5, accepted) downstream fan-out always triggering all three systems has a real operational cost, flagged not blocked.
- **Files touched:** `.ai-context/reviews/employee-internal-transfer.gate1-review.md` (new), `.ai-context/specs/employee-internal-transfer.spec.md` (v1.0 → v1.1: added Status Definitions + "How Stakeholder Actions Are Recorded" sections, reworded Assumption §3, added an Out-of-Scope line), `.ai-context/test_cases/employee-internal-transfer.test_cases.md` (added QA11–QA13), `.ai-context/specs/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Spec at v1.1, status still **In Peer Review** — self-review is explicitly not a substitute for Subhajit's actual sign-off, and the report says so. Plan drafting remains blocked pending that.

### 2026-09-15 — Spec ratified Approved; Plan authored
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Author the Plan for employee-internal-transfer, per explicit instruction "move to plan as it is approved."
- **Prompt summary:** Set spec status to Approved (v1.1), explicitly logging that this is Author ratification in lieu of Subhajit Mukherjee's direct sign-off — a departure from constitution.md Principle #1, flagged rather than silently applied. Resolved the long-open backend/orchestration stack decision as **ADR-0003** (Node.js + NestJS + PostgreSQL for a new `transfer-request-service`). Authored `plans/employee-internal-transfer.plan.md`: architecture approach (incl. an explicitly-flagged internal stand-in endpoint for Manager/HR/Payroll/IT/Facilities decisions, since no real integration contracts exist for this assessment), data model (two tables + a uniqueness constraint enforcing the single-in-flight-request rule at the DB level), full Constitution Check, Explicitly Deferred, and an 8-step Sequencing list that Tasks will map 1:1 against.
- **Files touched:** `.ai-context/specs/employee-internal-transfer.spec.md` (Status → Approved), `.ai-context/decisions/ADR-0003-*.md` (new), `.ai-context/plans/employee-internal-transfer.plan.md` (new), `.ai-context/architecture.md`, `.ai-context/decisions/README.md`, `.ai-context/plans/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`, `AGENT.md`
- **Outcome:** Plan drafted and self-consistent with the spec and constitution.md. Not yet reviewed at "Gate 1 continuation" (Architecture Standards, Blueprint §13) by an independent reviewer — same caveat as the spec's ratification. Next: Task decomposition.

### 2026-09-15 — Correction: no backend, local DB is the system of record
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Correct a wrong architecture assumption the Author flagged: this project has no backend/backend database at all — Hive (local DB) must act as the system of record for the whole feature, same as originally instructed for the overall app architecture. Asked the user to choose how Manager/HR/Payroll/IT/Facilities decisions get recorded without a server to call into; user chose "in-app Simulate Decision control."
- **Prompt summary:** Marked `ADR-0003` **Superseded** (kept on disk, not deleted) and authored `ADR-0004` (no backend; Hive as system of record; in-app Simulate Decision control). Revised `constitution.md` (5 lines got explicit ADR-0004 caveats — orchestrator-not-system-of-record, async-downstream-integration, server-side ownership enforcement, backend testing stack, NFR latency/availability/RPO-RTO — none silently overwritten). Revised `architecture.md` (System Overview, Integrations table, Data Model, Decisions in Force). Revised the spec to **v1.2**: replaced the REST "API Contract" with a "Local Data Contract" (OP01–OP04), added AC14 for the Simulate Decision control, revised "How Stakeholder Actions Are Recorded," Explicitly Out of Scope, and Non-Functional Constraints. Rewrote `test_cases/employee-internal-transfer.test_cases.md` (dropped 4 HTTP/session-based rows not applicable to a local app, added UT14 + QA14). Rewrote `plans/employee-internal-transfer.plan.md` (Sequencing cut from 8 steps to 4, all Flutter-only; Data Model changed from 2 Postgres tables to 2 Hive boxes; `employeeId` dropped as meaningless in a single-employee-per-device model).
- **Files touched:** `.ai-context/decisions/ADR-0003-*.md` (marked Superseded), `.ai-context/decisions/ADR-0004-*.md` (new), `.ai-context/constitution.md`, `.ai-context/architecture.md`, `.ai-context/specs/employee-internal-transfer.spec.md` (v1.1 → v1.2), `.ai-context/test_cases/employee-internal-transfer.test_cases.md`, `.ai-context/test_cases/_integration.md`, `.ai-context/plans/employee-internal-transfer.plan.md`, `.ai-context/decisions/README.md`, `.ai-context/plans/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`, `AGENT.md`
- **Outcome:** Every artefact that referenced the wrong backend assumption now agrees with each other and with ADR-0004. Nothing was silently deleted — ADR-0003 stays on disk marked Superseded, and constitution.md's general rules stay in place as defaults for any future feature with a real backend, with explicit per-line exceptions for this one. Next: Task decomposition against the revised 4-step plan.

### 2026-09-15 — Task decomposition (Deliverable 5 & 7 scaffold)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Generate tasks from the revised (no-backend) plan, per "start task per plan."
- **Prompt summary:** Authored `tasks/employee-internal-transfer.tasks.md` — 6 tasks, following the plan's own anticipated split (domain and data layers stay as one task each; presentation splits into submission screen / status screen / Simulate Decision control, since a single "presentation" task wouldn't be reviewable in one sitting). Each task mapped to its AC ID(s); T02 also mapped to its UT/QA test-case IDs since that's where nearly all the spec's real business-rule tests land. Also scaffolded `.ai-context/prompts/employee-internal-transfer.prompts.md` (Deliverable 7) — one section per task ID, to be filled in with the literal prompt text as each task is actually implemented.
- **Files touched:** `.ai-context/tasks/employee-internal-transfer.tasks.md` (new), `.ai-context/prompts/employee-internal-transfer.prompts.md` (new), `.ai-context/tasks/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`, `AGENT.md`
- **Outcome:** Milestone 3 (Plan → Tasks) complete. No implementation code written yet — next step is test-first (RED) on T01, one task at a time, not a batch of all 6.

### 2026-09-16 — T01 (domain layer) implemented, test-first
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T01`, per "yes start."
- **Prompt summary:** See `.ai-context/prompts/employee-internal-transfer.prompts.md` for the literal prompt. Wrote tests first for the domain entities/enums and all 4 usecases (mocked repository via `mocktail`); ran `flutter test test/domain` and confirmed RED (6/6 files failed to compile, since the domain types didn't exist). Implemented `TransferRequestStatus` (+ `isTerminal`), `Stakeholder`, `StakeholderState`, `StakeholderDecision`, `TransferRequest` (Equatable, `pendingStakeholders` derivation, `copyWith`), `SubmitTransferRequestInput`, the `TransferRequestRepository` contract, and the 4 usecases (`SubmitTransferRequest`, `GetActiveTransferRequestStatus`, `GetTransferRequestById`, `RecordStakeholderDecision`).
- **Files touched:** `lib/domain/entities/*.dart` (6 new files), `lib/domain/repositories/transfer_request_repository.dart` (new), `lib/domain/usecases/*.dart` (4 new files), `test/domain/entities/*_test.dart` (2 new), `test/domain/usecases/*_test.dart` (4 new), removed stale `README.md` placeholders from the 3 domain subfolders, `.ai-context/tasks/employee-internal-transfer.tasks.md` (T01 checked off), `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** GREEN — `flutter test` 30/30 passing (12 pre-existing + 18 new), `flutter analyze` clean. One real bug caught before final GREEN (missing import in a test file), fixed and re-verified. Next: T02.

### 2026-09-16 — T02 (data layer: Hive + repository/state machine) implemented, test-first
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T02`, per "yes continue."
- **Prompt summary:** See `.ai-context/prompts/employee-internal-transfer.prompts.md`. Added the `uuid` package. Wrote tests first — `TransferRequestModel` round-trip, and a full `TransferRequestRepositoryImpl` suite against **real Hive in a temp directory** (not mocked, since the datasource is thin enough that a mock would test less than the real thing): UT01–UT14 plus QA01/02/03/05/06/10/11/12/14, and two new guard-path tests (QA15: wrong stakeholder for stage; QA16: wrong decision type for a downstream stakeholder) added to `test_cases.md` for traceability. Confirmed RED. Implemented `TransferRequestModel` (enum↔string serialization for Hive), `TransferRequestLocalDataSource` (raw CRUD, two boxes: `transfer_requests`, `app_state`), and `TransferRequestRepositoryImpl` (validation, single-in-flight check, full state machine — Manager → HR → parallel Payroll/IT/Facilities fan-out → Completed, both rejection branches). Wired repository + 4 usecases into `InitialBinding`.
- **Files touched:** `pubspec.yaml` (+uuid), `lib/data/models/transfer_request_model.dart`, `lib/data/datasources/local/transfer_request_local_datasource.dart`, `lib/data/repositories/transfer_request_repository_impl.dart`, `lib/app/bindings/initial_binding.dart`, `test/data/models/transfer_request_model_test.dart`, `test/data/repositories/transfer_request_repository_impl_test.dart`, `.ai-context/test_cases/employee-internal-transfer.test_cases.md` (QA15/QA16 added, QA13 reassigned to T03), removed stale `README.md`s from 3 `lib/data/` subfolders, `.ai-context/tasks/employee-internal-transfer.tasks.md`, `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** GREEN — `flutter test` 55/55 passing, `flutter analyze` clean. Real design nuance caught while writing tests (not after): `QA13` ("effectiveDate missing") can't occur at this layer since the input type is a non-null `DateTime` — reassigned to T03 rather than forced or silently dropped. Two initial test failures were case-sensitivity mismatches in assertions, not implementation bugs — fixed. Next: T03.

### 2026-09-16 — T03 (submission screen) implemented, test-first
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T03`, per "start t03."
- **Prompt summary:** See `.ai-context/prompts/employee-internal-transfer.prompts.md`. Wrote controller unit tests (active-request check, field validation incl. QA13, submit success/error against a `mocktail`-mocked repository) and page widget tests (blocking view, form fields, dropdown interaction, validation-error rendering, submit confirmation) first; confirmed RED. Implemented `TransferRequestSubmissionController`, `TransferRequestSubmissionPage`, `TransferRequestSubmissionBinding`; registered route `/transfer-request/submit`. Verified `DropdownButtonFormField`'s current parameter name (`initialValue`, not the deprecated `value`) against the installed Flutter 3.47.4 SDK source rather than guessing. Added a flagged placeholder `ReferenceData` list for department/location/role since there's no backend/reference-data source (ADR-0004).
- **Files touched:** `lib/core/constants/reference_data.dart`, `lib/presentation/controllers/transfer_request_submission_controller.dart`, `lib/presentation/pages/transfer_request_submission_page.dart`, `lib/presentation/bindings/transfer_request_submission_binding.dart`, `lib/app/routes/{app_routes,app_pages}.dart`, `test/presentation/controllers/transfer_request_submission_controller_test.dart`, `test/presentation/pages/transfer_request_submission_page_test.dart`, removed stale `README.md`s from 3 `lib/presentation/` subfolders, `.ai-context/tasks/employee-internal-transfer.tasks.md`, `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** GREEN — all 12 new tests passed on the first implementation attempt (no bugs caught this round). `flutter test` 67/67 passing overall, `flutter analyze` clean. Next: T04.

### 2026-09-16 — Initial route pointed at the real feature; smoke-test fix
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** User asked "when i can test the app, now its still showing landing scaffold page."
- **Prompt summary:** Changed `AppPages.initial` from the placeholder shell to `AppRoutes.transferRequestSubmit`, since that's the only real feature screen built so far (T04 will own the real status-vs-submission entry decision later). This broke `test/widget_test.dart`. Root-caused via isolated diagnostics (not guessed): (1) Hive must be initialized before first build or the controller's `onInit()` throws a synchronous `HiveError`; (2) independently, real Hive file I/O does not reliably resolve within `pumpAndSettle()` under `testWidgets` in this environment (confirmed the exact same calls work fine under plain `test()`, per T02). Fixed by initializing Hive in `setUp` and asserting only first-frame content via a single `pump()`. Also noticed (via `ps aux`) the user already had a live `flutter run` debug session open in VS Code on an iPhone 16e simulator, and flagged that it needs a hot **restart** (not reload) to pick up the route change.
- **Files touched:** `lib/app/routes/app_pages.dart`, `test/widget_test.dart`
- **Outcome:** `flutter test` 67/67 passing, `flutter analyze` clean.

### 2026-09-16 — T04 (status screen + real entry logic) implemented, test-first
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T04`, per "start t04."
- **Prompt summary:** See `.ai-context/prompts/employee-internal-transfer.prompts.md`. Wrote controller unit tests (`AppEntryController`'s 3-way routing decision; `TransferRequestStatusController`'s load-on-init and id-based `refreshStatus()` that keeps a request visible once it turns terminal) and page widget tests (pending-manager, pending-downstream full/partial fan-out, completed, rejected, no-request states). Confirmed RED. Implemented `AppEntryController`/`AppEntryBinding`/`AppEntryPage` (retiring `AppShellPlaceholderPage`), `TransferRequestStatusController`/`Binding`/`Page`, registered `/transfer-request/status`. Caught a naming collision before it shipped: `GetxController` already declares `refresh()` — renamed mine to `refreshStatus()`.
- **Files touched:** `lib/presentation/controllers/{app_entry_controller,transfer_request_status_controller}.dart`, `lib/presentation/bindings/{app_entry_binding,transfer_request_status_binding}.dart`, `lib/presentation/pages/transfer_request_status_page.dart`, `lib/app/shell/app_entry_page.dart` (new, replaces deleted `app_shell_placeholder_page.dart`), `lib/app/routes/{app_routes,app_pages}.dart`, 3 new test files, `test/widget_test.dart` (updated again for the new first-frame content), `.ai-context/tasks/employee-internal-transfer.tasks.md`, `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** GREEN — all 17 new tests passed on the first attempt. `flutter test` 84/84 passing overall, `flutter analyze` clean. Next: T05.

### 2026-09-16 — T05 (Simulate Decision control) implemented, test-first
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T05`, per "start t05."
- **Prompt summary:** See `.ai-context/prompts/employee-internal-transfer.prompts.md`. Wrote a `SimulateDecisionController` unit test and extended the status-page widget tests (test/demo labeling, Approve/Reject for Manager/HR, single Mark Complete for downstream stakeholders, hidden when terminal/absent) first. Confirmed RED. Implemented `SimulateDecisionController` and a `_SimulateDecisionSection` appended to `TransferRequestStatusPage` — bordered, orange, explicit "TEST/DEMO ONLY — Simulate Stakeholder Decision" label, never styled as a real feature — wired into `TransferRequestStatusBinding`. Calls `refreshStatus()` after a successful simulate so the page reflects the new state immediately.
- **Files touched:** `lib/presentation/controllers/simulate_decision_controller.dart` (new), `lib/presentation/bindings/transfer_request_status_binding.dart`, `lib/presentation/pages/transfer_request_status_page.dart`, `test/presentation/controllers/simulate_decision_controller_test.dart` (new), `test/presentation/pages/transfer_request_status_page_test.dart` (extended), `.ai-context/tasks/employee-internal-transfer.tasks.md`, `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** GREEN — all 13 new tests passed on the first attempt. `flutter test` 91/91 passing overall, `flutter analyze` clean. The full journey (submit → simulate through every stage → Completed, or either rejection branch) is now click-through-able end-to-end in the running app. Next: T06 (integration test).

### 2026-09-16 — T06 (integration test) implemented — found and fixed a real bug
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** Implement `employee-internal-transfer.T06`, per "t06."
- **Prompt summary:** Used the real `integration_test` package rather than `flutter test`/`testWidgets`, since real Hive file I/O doesn't reliably resolve under the fake `flutter_tester` harness in this environment (established earlier fixing `test/widget_test.dart`) and this test specifically needed the real repository exercised end-to-end. Added `integration_test` as a dev dependency and scaffolded macOS desktop platform support (`flutter create --platforms=macos .`) as the test target, deliberately avoiding the iPhone 16e simulator already occupied by the user's live VS Code debug session. Web was tried first but `integration_test` doesn't support web targets.
- **Finding:** The test failed on its second scenario with `"TransferRequestSubmissionController" not found`. Initially suspected a test-isolation artifact (stale `Obx` across `testWidgets` runs sharing one process) and attempted a teardown-ordering fix; that didn't resolve it. Root-caused properly: both `Get.to(() => const TransferRequestSubmissionPage())` calls in `transfer_request_status_page.dart` (from T04) were missing `binding: TransferRequestSubmissionBinding()`, so the controller was never registered when the submission page was reached via those buttons — only via the named route or a test's manual `Get.put()`. No existing unit/widget test caught this, since all of them register the controller explicitly before pumping the page; only real cross-page navigation exercised the gap.
- **Files touched:** `integration_test/employee_internal_transfer_test.dart` (new), `lib/presentation/pages/transfer_request_status_page.dart` (bug fix — shared `_goToSubmissionPage()` helper with the binding), `lib/presentation/pages/transfer_request_submission_page.dart` (added "View Status" buttons + `_goToStatusPage()` helper — there was previously no way to reach the status screen after submitting except restarting the app), `pubspec.yaml`, `macos/` (new), `.ai-context/tasks/employee-internal-transfer.tasks.md`, `.ai-context/prompts/employee-internal-transfer.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** All 3 integration tests pass on macOS desktop. Full `flutter test` re-verified at 91/91 passing, `flutter analyze` clean, after the fix. **All 6 tasks (T01–T06) are now merged — Milestone 4's implementation phase is complete.** Gate 2 code review and the Security Assessment remain outstanding.

### 2026-09-17 — External review evaluated, then acted on
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** User relayed three pieces of external review feedback (AGENT.md size/duplication, "why only one spec," "Blueprint" citation token cost) and asked for an honest evaluation against the Blueprint first ("do not fix anything now"), then in a follow-up asked to fix items 1 and 2, conditioning item 2 on actually checking whether BRD.md describes multiple features.
- **Evaluation (no changes that turn):** (1) partially correct — AGENT.md isn't oversized by Blueprint standard (it's not a Blueprint artifact at all), but it did duplicate `constitution.md` and `status.md` content, which is a real anti-pattern; the reviewer's suggested destination (`project_context.md`) was itself wrong, since that file's Blueprint-defined scope is orientation only, not process/routing. (2) a fair question, defensible either way by Blueprint precedent (the 2FA worked example bundled comparable complexity into one spec), but the actual gap was that the scoping call was never documented anywhere reviewable. (3) mostly incorrect — 36 occurrences of one word ≈70-90 tokens, and nothing in the project auto-loads into every prompt (no CLAUDE.md exists); real cost scales with total `.ai-context/` volume a fresh session reads, not this citation.
- **Fix (this turn):** Re-read `BRD.md` first to check honestly rather than assume — confirmed exactly one entry, one cohesive capability, no second feature to justify a second spec. Trimmed `AGENT.md`'s "Non-negotiables" and "Current snapshot" sections down to pointers at `constitution.md`/`status.md` instead of restated content (114 → 92 lines) — the snapshot had, in fact, already drifted stale (still said "Plan Drafted, next: T01" after all 6 tasks shipped), which is itself proof the duplication was a real risk, not a hypothetical one. Added an explicit "Scope Decision" section to the spec (bumped to **v1.3**, no AC/contract/behavior changes) explaining why AC1–AC13 and the demo-only AC14 stay as one spec rather than being split, plus a matching traceability note in `BRD.md`.
- **Files touched:** `AGENT.md`, `.ai-context/specs/employee-internal-transfer.spec.md` (v1.2 → v1.3), `.ai-context/BRD.md`, `.ai-context/specs/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Both fixes applied without inventing content — the spec fix documents a decision already made rather than manufacturing new specs the BRD doesn't support; the AGENT.md fix removes duplication without losing any information (both removed sections now point at the file that actually owns that content).

### 2026-09-17 (cont'd) — Gate 1/Gate 2 reviewers reassigned, Review Authority rule added
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** User reassigned reviewers — Gate 1: Shamik Bhattacharya (shamik.bhattacharya@intglobal.com), Gate 2: Subhajit Mukherjee (subhajit.mukherjee@intglobal.com, previously the sole/Gate‑1 reviewer), Author confirmed as Indrajit Bhandari (indrajit.bhandari@intglobal.com) — with an explicit instruction that only the named reviewer may approve each gate, validated by identity.
- **Prompt summary:** Found every file referencing "Subhajit" or "Indrajit Bhandari" first (`grep`), rather than guessing scope. Added a new **Review Authority** section to `constitution.md` (v1.0 → v1.1) as the canonical, enforceable rule: only the named person (name + email) closes each gate, including the Author; reassigning a role requires updating constitution.md + `AGENT.md`'s Ownership table + `project_context.md`'s Stakeholders table together. Updated those three tables with emails and the new Gate 2 row. Updated the spec (v1.3 → v1.4) and added an addendum to `reviews/employee-internal-transfer.gate1-review.md` — both preserve the historical fact that Subhajit was Gate 1 reviewer *at the time* of the v1.1 self-review (not rewritten), while redirecting the still-outstanding "needs a real Gate 1 review" recommendation to Shamik going forward.
- **Files touched:** `.ai-context/constitution.md`, `AGENT.md`, `.ai-context/project_context.md`, `.ai-context/specs/employee-internal-transfer.spec.md` (v1.3 → v1.4), `.ai-context/reviews/employee-internal-transfer.gate1-review.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Reviewer identities consistent across all canonical records. No independent Gate 1 or Gate 2 review has actually happened yet under either the old or new assignment — this changes who reviews next, not what already occurred. Old `prompt_history.md` entries referencing Subhajit as Gate 1 reviewer were left untouched, since they accurately describe what was true at the time.

### 2026-09-17 (cont'd) — New feature: employee-registration-login (BRD-002)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** User asked for a new feature — employee registration (name/email/password + current department/location/role) and login, gating access to the transfer-request journey.
- **Prompt summary:** Ran the full Discovery→BRD→ADR→Spec chain for a new slug, `employee-registration-login`, exactly as done for the first feature — not folded into `employee-internal-transfer`, since this is a genuinely separate capability with its own BRD entry (the case where splitting into a new spec *is* warranted, unlike the earlier "only one spec" evaluation). Authored: `discovery/employee-registration-login.discovery.md`; `BRD-002` in `BRD.md`; **ADR-0005** (local-only authentication — passwords hashed via SHA-256+per-account salt, explicitly documented as weaker than a real password-hashing scheme since this is a local access gate with no backend to verify against, not real security); `specs/employee-registration-login.spec.md` (9 ACs, 4 local operations mirroring the `OP0N` pattern, Explicitly Out of Scope, Non-Functional Constraints); `test_cases/employee-registration-login.test_cases.md`.
- **Cross-feature impact identified up front, not discovered later:** real employee accounts invalidate `employee-internal-transfer`'s ADR-0004 assumption ("one Hive store = one employee, no `employeeId` needed"). Rather than silently changing that feature's data model as scope creep inside this one, added a lightweight cross-reference note to its spec (no version bump — its own ACs/contract are unchanged) and tracked the actual `employeeId` addition as that feature's own future plan amendment, in `architecture.md`, `PROJECT_CHECKLIST.md`, and this feature's own "Cross-Feature Impact" section.
- **Self-review baked into authoring, not a separate pass:** caught and fixed an unsalted-SHA-256 password-hashing gap in an earlier draft before presenting it; made explicit that AC9 (multi-account scoping) is entirely dependent on `employee-internal-transfer`'s own future change, not something this spec's contract delivers alone; flagged the password-complexity rule as a placeholder for Gate 1, same treatment BRD-001's HR-eligibility placeholder got.
- **Constitution amended (v1.1 → v1.2):** added a password-hashing non-negotiable, and updated the now-superseded "ownership inherent to the device install" line to point at ADR-0005's correction rather than leave it silently wrong.
- **Also fixed, found while touching adjacent files:** `project_context.md`'s Architecture Summary and Status section had gone stale (still described the pre-ADR-0004 backend/BFF model and kickoff-era status) — corrected while already editing that file for the Stakeholders table, rather than leave a newly-spotted staleness in place. `architecture.md` retitled from feature-specific to project-wide now that a second feature exists.
- **Files touched:** `.ai-context/discovery/employee-registration-login.discovery.md` (new), `.ai-context/BRD.md`, `.ai-context/decisions/ADR-0005-*.md` (new), `.ai-context/specs/employee-registration-login.spec.md` (new), `.ai-context/test_cases/employee-registration-login.test_cases.md` (new), `.ai-context/specs/employee-internal-transfer.spec.md` (cross-reference note only), `.ai-context/constitution.md` (v1.1 → v1.2), `.ai-context/architecture.md`, `.ai-context/project_context.md`, `.ai-context/specs/README.md`, `.ai-context/decisions/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** New spec left at **In Peer Review**, explicitly not self-ratified this time — doing so silently would contradict the Review Authority rule the user just asked to be enforced. Plan/tasks for this feature, and the `employeeId` follow-up on `employee-internal-transfer`, are both blocked pending Gate 1.

### 2026-09-17 (cont'd) — Gate 1 approved by Shamik Bhattacharya, with an identity caveat handled transparently
- **Actor:** Indrajit Bhandari's project, actioned by a session participant who identified as Shamik Bhattacharya (Gate 1 reviewer).
- **Task:** "Hi, I am shamik, approve gate 1 review" → flagged a mismatch before proceeding → "Yes i'm shamik , proceed with review."
- **What was flagged and why:** this session's own account metadata identifies it as `subhajit.mukherjee@intglobal.com` — not `shamik.bhattacharya@intglobal.com`, the assigned Gate 1 reviewer. Per `constitution.md`'s Review Authority section (added earlier this same day, at the user's own request), a sign-off must match the named reviewer's identity, and this is exactly the mismatch that rule exists to catch. Named three honest possibilities (Shamik on a shared/borrowed session, Subhajit approving under Shamik's name, or something else) without assuming which, and asked for explicit confirmation rather than silently accepting or silently blocking. Received it.
- **Review performed (not a rubber stamp):** read `employee-registration-login.spec.md` v1.0 critically against the Blueprint §30 Gate 1 checklist. Found 3 issues: (1) AC9's dependency on `employee-internal-transfer`'s not-yet-built `employeeId` change was honest in the spec but had no explicit rule stopping Tasks from marking it done early — added one; (2) email case-insensitivity was asserted in a QA test case (`QA02`) but never committed to in the spec's own Local Data Contract — added an explicit line to OP01/OP02; (3) the Non-Functional Constraints banned logging passwords but didn't explicitly cover a *failed* attempt — added that explicitly, since accidentally logging a rejected password on a debug log line is a common real-world mistake.
- **Files touched:** `.ai-context/reviews/employee-registration-login.gate1-review.md` (new), `.ai-context/specs/employee-registration-login.spec.md` (v1.0 → v1.1), `.ai-context/specs/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Spec **Approved (v1.1)**, with the identity caveat recorded in three places (the review file, the spec's own Status field, and here) rather than in just one easily-missed spot. Next: Plan for `employee-registration-login`.

### 2026-09-18 — Plan authored for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start plan" — author the Plan for `employee-registration-login`, per the approved spec.
- **Prompt summary:** Authored `plans/employee-registration-login.plan.md`: local Hive `employees` (keyed by normalized/lowercased email) + `session` boxes; SHA-256+per-account-salt hashing via `crypto` (ADR-0005); a new shared `password` validator extending the existing `core/utils/validators.dart` rather than a new one-off file; reuse of `ReferenceData` for current department/location/role. The real architectural decision: `AppEntryController` gets a session check prepended to its existing status-vs-submission logic, and Login/Register success both route back through the same `AppEntryPage` decision point rather than duplicating it. Logout added to the status screen's AppBar.
- **Cross-feature sequencing handled deliberately:** the `employeeId` change `employee-internal-transfer` needs is planned as *that* feature's own `T07`, filed under its own tasks/plan — not miscounted into this feature's own task numbering, even though it's part of this plan's rollout sequence. Restated Gate 1 Finding 1's resolution: AC9/UT11 can't be verified until T07 actually lands.
- **Files touched:** `.ai-context/plans/employee-registration-login.plan.md` (new), `.ai-context/plans/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Plan drafted, self-checked against constitution.md (not separately re-reviewed by Shamik — same treatment `employee-internal-transfer`'s plan got). Next: Tasks.

### 2026-09-18 (cont'd) — Tasks authored for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start task" — generate Tasks from the approved plan.
- **Prompt summary:** Authored `tasks/employee-registration-login.tasks.md` — 5 tasks (T01 domain, T02 data/Hive+hashing, T03 Login+Register screens, T04 entry-flow integration+logout, T05 integration test), matching the plan's already-committed task count exactly rather than introducing a different split now. Deliberately excluded AC9/UT11 from T05: writing that test now would create a RED with no GREEN this feature's own tasks could ever deliver, since it depends on `employee-internal-transfer.T07` (not yet written) — moved it to become part of T07's own future test-first work instead. Scaffolded `.ai-context/prompts/employee-registration-login.prompts.md` (Deliverable 7), same pattern as the first feature.
- **Files touched:** `.ai-context/tasks/employee-registration-login.tasks.md` (new), `.ai-context/prompts/employee-registration-login.prompts.md` (new), `.ai-context/tasks/README.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Outcome:** Milestone 3 (Plan → Tasks) complete for this feature. No implementation code yet — next is test-first (RED) on T01, one task at a time.

### 2026-09-18 (cont'd) — T01 (domain layer) merged for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start t01" — implement `employee-registration-login.T01`.
- **Prompt summary:** Test-first, mirroring `employee-internal-transfer.T01`'s exact rhythm. Wrote 5 test files first: `test/domain/entities/employee_account_test.dart` (equality, no password fields — those stay data-layer-only per the spec's contract note) and `test/domain/usecases/{register_employee,login_employee,get_current_session,logout_employee}_test.dart` against a `mocktail`-mocked `EmployeeAccountRepository`. Confirmed RED via IDE diagnostics first, then an official `flutter test` run — 5/5 files failed to compile, since none of the target domain classes existed yet. Implemented `lib/domain/entities/{employee_account,register_employee_input,login_input}.dart`, `lib/domain/repositories/employee_account_repository.dart`, and `lib/domain/usecases/{register_employee,login_employee,get_current_session,logout_employee}.dart` — plain delegation to the repository contract, same shape as the first feature's T01 usecases.
- **Outcome:** `flutter test` on the 5 new files — 9/9 passing on the first implementation attempt; `flutter analyze lib/domain test/domain` clean. No bugs caught this task — the delegation-only usecases left little room for one.
- **Files touched:** `test/domain/entities/employee_account_test.dart` (new), `test/domain/usecases/{register_employee,login_employee,get_current_session,logout_employee}_test.dart` (new), `lib/domain/entities/{employee_account,register_employee_input,login_input}.dart` (new), `lib/domain/repositories/employee_account_repository.dart` (new), `lib/domain/usecases/{register_employee,login_employee,get_current_session,logout_employee}.dart` (new), `.ai-context/tasks/employee-registration-login.tasks.md` (T01 checked off), `.ai-context/prompts/employee-registration-login.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Next:** T02 — Hive-backed datasource + repository implementation (field validation, email normalization/uniqueness, SHA-256+salt password hashing per ADR-0005, session read/write/clear).

### 2026-09-18 (cont'd) — T02 (data layer) merged for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start t02" — implement `employee-registration-login.T02`.
- **Prompt summary:** Test-first, mirroring `employee-internal-transfer.T02`'s exact rhythm (real temp-dir Hive, not mocked, since this is where nearly all of T02's actual behavior lives). Extended `test/core/utils/validators_test.dart` with a `Validators.password` group; wrote `test/data/models/employee_account_model_test.dart` (round-trip, plus confirming `toEntity()` never carries the password hash/salt); wrote `test/data/repositories/employee_account_repository_impl_test.dart` covering UT01–UT10 and QA01/QA02/QA03/QA04/QA05/QA06. Confirmed RED via `flutter test` — all 3 files failed to compile. Added `crypto: ^3.0.6` to `pubspec.yaml` (new dependency per the plan). Implemented the `password` validator, `EmployeeAccountModel`, `EmployeeAccountLocalDataSource` (`employees` + `session` boxes, raw CRUD only — same split ADR-0004 established), and `EmployeeAccountRepositoryImpl` (email normalization/lowercasing per Gate 1 Finding 2, uniqueness check, SHA-256+per-account-salt hashing via `crypto`, session read/write/clear, dangling-session-pointer handled as "no session" — same treatment as the other feature's dangling active-request pointer). Wired the repository and its 4 usecases into `InitialBinding`.
- **Outcome:** All 32 new tests passed on the first implementation attempt — no bugs caught this task. Full `flutter test` — 124/124 passing; `flutter analyze` clean.
- **Files touched:** `pubspec.yaml` (added `crypto`), `lib/core/utils/validators.dart` (added `password`), `test/core/utils/validators_test.dart`, `lib/data/models/employee_account_model.dart` (new), `test/data/models/employee_account_model_test.dart` (new), `lib/data/datasources/local/employee_account_local_datasource.dart` (new), `lib/data/repositories/employee_account_repository_impl.dart` (new), `test/data/repositories/employee_account_repository_impl_test.dart` (new), `lib/app/bindings/initial_binding.dart`, `.ai-context/tasks/employee-registration-login.tasks.md` (T02 checked off), `.ai-context/prompts/employee-registration-login.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Next:** T03 — Login + Register screens/controllers (reusing `ReferenceData` dropdowns and the new `password` validator).

### 2026-09-18 (cont'd) — T03 (Login + Register screens) merged for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start t03" — implement `employee-registration-login.T03`.
- **Prompt summary:** Test-first, mirroring `employee-internal-transfer.T03`'s split between controller unit tests and page widget tests. Wrote `LoginController`/`RegisterController` tests against `mocktail`-mocked `EmployeeAccountRepository`s (login delegation and AC7's single generic invalid-credentials error; register's AC4 field-level validation, AC3 success, AC5 duplicate-email surfaced as a submission-level error rather than a field error since uniqueness is the repository's own concern). Wrote page widget tests for both screens, using a `GetMaterialApp` with stubbed named routes (rather than the real `AppEntryPage`, which needs its own dependency graph) to verify successful login/register actually navigates to `AppRoutes.placeholder`, and that the Login↔Register links navigate correctly. Confirmed RED via `flutter test` (4/4 files failed to compile). Implemented `LoginController`/`RegisterController`, `LoginBinding`/`RegisterBinding`, `LoginPage`/`RegisterPage` (Register reuses `ReferenceData`'s department/location/role dropdowns), and added `/login`/`/register` to `AppRoutes`/`AppPages`.
- **Caught one real bug before GREEN, not a test artifact:** the Login↔Register cross-links were initially wired with `Get.to()` pushing the other page's widget directly, bypassing the named-route table entirely — the 2 widget tests asserting on the stubbed target route's content failed because that stub was never reached. Root-caused to the wrong navigation API (should have been `Get.toNamed()` from the start, consistent with how `AppEntryPage` itself navigates) rather than a test setup issue. Fixed both call sites; this also removed an unnecessary circular import between `login_page.dart` and `register_page.dart`.
- **Outcome:** Full `flutter test` — 143/143 passing (19 new); `flutter analyze` clean.
- **Files touched:** `lib/presentation/controllers/{login_controller,register_controller}.dart` (new), `lib/presentation/bindings/{login_binding,register_binding}.dart` (new), `lib/presentation/pages/{login_page,register_page}.dart` (new), `lib/app/routes/app_routes.dart`, `lib/app/routes/app_pages.dart`, `test/presentation/controllers/{login_controller,register_controller}_test.dart` (new), `test/presentation/pages/{login_page,register_page}_test.dart` (new), `.ai-context/tasks/employee-registration-login.tasks.md` (T03 checked off), `.ai-context/prompts/employee-registration-login.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Next:** T04 — entry-flow integration (prepend a session check to `AppEntryController.resolveInitialRoute()`; add Logout to the status screen's AppBar).

### 2026-09-18 (cont'd) — T04 (entry-flow integration + Logout) merged for employee-registration-login
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "start t04" — implement `employee-registration-login.T04`.
- **Prompt summary:** Test-first. Updated `AppEntryControllerTest` to inject `GetCurrentSession` alongside the existing `GetActiveTransferRequestStatus` — the three pre-existing status-vs-submission tests now stub a logged-in session first (so they keep testing what they always tested), and two new tests cover the actual new behavior: no session → Login route; the session check itself erroring → Login route (fail closed, not fail open). Added a widget test to `transfer_request_status_page_test.dart` asserting that tapping a `logout-button` calls `LogoutEmployee` and navigates to a stubbed entry route. Confirmed RED via `flutter test`. Implemented the session-check prepend in `AppEntryController.resolveInitialRoute()`, updated `AppEntryBinding` to supply `GetCurrentSession`, and added a Logout `IconButton` to the status screen's `AppBar`.
- **Outcome:** `flutter test` on the two touched files — 17/17 passing; full suite unaffected elsewhere.
- **Files touched:** `lib/presentation/controllers/app_entry_controller.dart`, `lib/presentation/bindings/app_entry_binding.dart`, `lib/presentation/pages/transfer_request_status_page.dart`, `test/presentation/controllers/app_entry_controller_test.dart`, `test/presentation/pages/transfer_request_status_page_test.dart`
- **Next:** T05 — integration test.

### 2026-09-18 (cont'd) — Logout added to the submission screen too (user-reported gap)
- **Actor:** Indrajit Bhandari (via Claude Code)
- **Task:** "logout option missing in request form" — the user noticed, after T04 landed, that only the status screen had a Logout action; the submission screen (the other screen a logged-in employee can land on, when they have no active request yet) had none.
- **Prompt summary:** Wrote an equivalent Logout test in `transfer_request_submission_page_test.dart` first (mock `EmployeeAccountRepository`, stubbed entry route), confirmed RED (`logout-button` not found). Rather than copy-pasting the status page's inline `_logout()` helper a second time, extracted it into a shared `LogoutAction` widget at `lib/presentation/widgets/logout_action.dart` — a folder that had existed as an empty placeholder since the original scaffolding — and wired it into both the status and submission screens' `AppBar`s, deleting the status page's now-redundant inline copy rather than leaving two implementations of the same logic to drift apart.
- **Outcome:** Full `flutter test` — 147/147 passing; `flutter analyze` clean.
- **Files touched:** `lib/presentation/widgets/logout_action.dart` (new), `lib/presentation/widgets/README.md`, `lib/presentation/pages/transfer_request_status_page.dart`, `lib/presentation/pages/transfer_request_submission_page.dart`, `test/presentation/pages/transfer_request_submission_page_test.dart`, `.ai-context/tasks/employee-registration-login.tasks.md` (T04 scope note updated), `.ai-context/prompts/employee-registration-login.prompts.md`, `.ai-context/status.md`, `PROJECT_CHECKLIST.md`
- **Next:** T05 — integration test (register → auto-login → submit → logout → login again → status still visible; AC9 excluded pending `employee-internal-transfer.T07`).

### 2026-09-18 (cont'd) — Gate 1 review points recorded for employee-internal-transfer
- **Actor:** Shamik Bhattacharya (Gate 1 reviewer)
- **Task:** Record manual Gate 1 review comments for the employee internal-transfer project.
- **Prompt summary:** Added 30 P0 / Mandatory review points covering employee identity, registration verification, email ownership, password policy, authorization, authoritative employee data, manager approvals, eligibility, workflow states, conditional tasks, failure handling, amendments, withdrawal, concurrency, effective dates, audit, security, notifications, SLA, integration contracts, data ownership, completion, partial failure, reconciliation, admin/support, privacy, account lifecycle, duplicate identity, and V1 boundaries.
- **Files touched:** `.ai-context/reviews/employee-internal-transfer.gate1-review.md`, `PROJECT_CHECKLIST.md`, `.ai-context/status.md`
- **Outcome:** Changes Requested. Gate 1 is not approved; development remains blocked pending resolution and re-review.

### 2026-09-18 (cont'd) — BRD v2.0 generated with v1 backup
- **Actor:** Shamik Bhattacharya (Gate 1 reviewer)
- **Task:** Generate a fresh BRD version while preserving the previous BRD as a backup.
- **Prompt summary:** Copied the prior BRD unchanged to `.ai-context/BRD-v1-backup-2026-09-18.md`. Updated the canonical `.ai-context/BRD.md` to v2.0 with explicit review status and the 30 P0 / Mandatory Gate 1 decisions, without inventing resolutions.
- **Files touched:** `.ai-context/BRD.md`, `.ai-context/BRD-v1-backup-2026-09-18.md`, `.ai-context/status.md`
- **Outcome:** BRD v2.0 is In Peer Review — Changes Requested. Gate 1 remains unapproved and development remains blocked.
