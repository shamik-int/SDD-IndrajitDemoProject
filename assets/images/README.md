# assets/images/

All static image assets (PNG/SVG/JPEG) for the app. Declare each new file (or the
whole folder) under `flutter: assets:` in `pubspec.yaml` once the Flutter project
is scaffolded (implementation stage).
