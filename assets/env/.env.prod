# Employee Internal Transfer — PROD environment configuration
# Non-sensitive configuration only. Secrets/API keys are NEVER stored here — see
# .ai-context/constitution.md Security Posture. Real credentials are injected via
# the organisation's secrets manager / CI pipeline at build time.

ENV_NAME=PROD
API_BASE_URL=https://onepoint-portal.example.com/api
ENABLE_LOGGING=false
